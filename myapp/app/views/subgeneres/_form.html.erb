<div class="row">
  <div class="col-md-6 col-md-offset-3 well" >
<%= form_with(model: subgenere, local: true) do |form| %>
  <% if subgenere.errors.any? %>
    <div id="error_explanation">
      <h2 class="text-center"><%= pluralize(subgenere.errors.count, "error") %> prohibited this subgenere from being saved:</h2>

      <ul>
      <% subgenere.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :name %>
    <%= form.text_field :name, id: :subgenere_name %>
  </div>
  

  <div>
    <%= form.submit %>
  </div>
  
<% end %>
